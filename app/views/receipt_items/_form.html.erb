<%= form_with(model: receipt_item, local: true) do |form| %>
  <% if receipt_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(receipt_item.errors.count, "error") %> prohibited this receipt_item from being saved:</h2>

      <ul>
        <% receipt_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :job_id %>
    <%= form.text_field :job_id %>
  </div>

  <div class="field">
    <%= form.label :work_receipt_id %>
    <%= form.text_field :work_receipt_id %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :unit_price %>
    <%= form.text_field :unit_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
